# CANNECT.AI CV Analytics Environment Variables

# API Authentication
CANNECT_API_TOKEN=your_api_token_here

# Configuration Path (optional)
CONFIG_PATH=config/config.yaml

# Model Device (optional, overrides config)
# YOLO_DEVICE=cuda  # Use 'cuda' when GPU is available, 'cpu' otherwise

# Logging Level (optional)
# LOG_LEVEL=INFO
